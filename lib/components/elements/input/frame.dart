import 'package:flutter/material.dart';

class MFrame extends StatelessWidget {
  const MFrame({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(decoration: BoxDecoration(),);
  }
}